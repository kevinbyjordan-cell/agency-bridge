# SITE · 2026-06-22T09:43:14 · 0e9ce772

**Run:** reação à mudança do bridge (o ADS travou o CPC) + confirmação do merge do PR #1.

**Mudança lida no bridge:** ADS aplicou **cap de CPC $5** (bidding MAXIMIZE_CONVERSIONS → MAXIMIZE_CLICKS,
teto $5/clique) — metade do ADS feita; ambos os lados aguardavam o merge do PR #1.

**O que confirmei/fiz:**
- PR **petoothfairy-site#1 = MERGED** (squash `999627f` em `main` pelo dono). Confirmei em `origin/main`:
  `Hero.tsx` com `href="#quote"` ×2 + `StickyMobileCTA.tsx` com `/#quote`. Netlify auto-deploy em produção.
- Sincronizei local (`963303f..999627f` fast-forward) e apaguei a branch mergeada.
- **Verifiquei a instrumentação de medição** (`BookingForm.tsx`): `referralSource` no /book é o campo
  "How did you hear about us?" (escolha do cliente, default vazio) — NÃO uma etiqueta de funil. O quiz crava
  `referralSource:"quiz"`. **Decisão: NÃO mexer no modelo de dados** (seria superdimensionar) — a separação
  quiz×form já é possível: `=="quiz"` → quiz; resto → form; pago por keyword via gclid. Deixei a **receita no
  Snapshot** do board.

**Estado do loop:** ambas as metades no ar (SITE: re-rota+dead-click · ADS: cap $5). **Modo MEDIÇÃO** —
congelado baseline `0 conv / 14 cliques (2d)` pré-PR#1; budget $30/dia até a 1ª conversão.

**Pendências sinalizadas (não-código / próximas propostas):**
- `CLARITY_DATA_EXPORT_TOKEN` na Netlify (prod) = **ação do dono** (segredo; agente não insere credencial).
- Jordana teaser mais cedo em páginas de anúncio = vira proposta se o dono quiser.

**Rollback handle:** reverter `999627f` em `main` (PR #1) restaura `href="/book"` + elementos sem link.
**as_of:** Clarity 3d 2026-06-22 · Ads 14 cliques/0 conv (2d) · ADS cap $5 (06-22).
