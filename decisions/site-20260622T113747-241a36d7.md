# SITE · 2026-06-22T15:37:47Z · 241a36d7

**Run:** conferência geral a pedido do dono ("confira agora como está") + fechamento do #6.

**Estado conferido AO VIVO:**
- **PR petoothfairy-site#2 = MERGED** (mergedAt 15:28Z, squash `90b7c07`) — o dono pediu e o agente ADS executou o
  merge; script `scripts/bridge-heartbeat.mjs` agora **permanente na `main`** (confirmado em `git ls-tree origin/main`).
- **Task `PTF-Bridge-Heartbeat` registrada** (State=Ready) — **1ª execução OK (LastTaskResult=0)** às ~11:35,
  **NextRunTime ~13:34**. O commit local da task (`bf362e7`, `--no-push`) estava aguardando sync → **empurrei agora**
  (é o desenho: a task commita local e sincroniza no próximo run interativo).
- **Heartbeat ao vivo:** site-up 200 · bookings_today 0 · `ok=true` · sem alertas.

**Resultado:** **#6 COMPLETO/operacional.** Watchdog 2h rodando **dos 2 lados** (SITE + ADS). Loop 100% no ar em
**modo medição** (esperando 1ªs conversões; baseline congelado).

**Pendências (não-bloqueantes):** dono pode dar `git pull` no `main` local p/ alinhar (o script restaurado staged
vira committed). Segurança: **rotacionar a chave de service-account** (apareceu em print) — recomendado, não urgente.

**Boundary:** só leitura/relatório + sync do bridge; nada mudou no site nem na conta.
