# SITE · 2026-06-22T09:58:47 · b5ee6ee6

**Run:** ligar os "olhos" do loop em produção (Clarity Data Export token).

**Contexto:** dono criou a env var `CLARITY_DATA_EXPORT_TOKEN` na Netlify (eu **não** insiro segredo —
regra fixa; o dono colou o valor). Eu fiz o resto **sem tocar no valor**.

**O que fiz:**
- Confirmei a presença da chave **sem imprimir o valor** (`env:list --json` → match só do NOME): `KEY_PRESENT=True`.
- Confirmei **sem vazamento** `NEXT_PUBLIC_CLARITY_DATA_EXPORT` (server-only correto): `False`.
- Disparei **rebuild de produção** git-based: `netlify api createSiteBuild` (site `610d60b7…`).
  Deploy `6a393f17e297840081ff091d` → poll → **state=ready** (~1 min).
- Atualizei o board: ⚠️ do token → ✅ (olhos do loop ligados em prod); + linha no "Feito".

**Limite honesto:** a confirmação visual final (a aba "Clarity" do `/admin` mostrando dado ao vivo) precisa
do **dono logado** — o endpoint `/api/admin/clarity-insights` é admin-gated; não dá pra testar sem login.

**Boundary respeitado:** não inseri o segredo (ação do dono); só operei deploy + verificação read-only.
**Rollback handle:** se o token estiver errado, a aba cai no fallback (cache/stale/mock) sem quebrar o site;
o dono pode reeditar a env var e eu redisparo o deploy.

**Novo ticket recebido:** `ADS→SITE #6` (heartbeat read-only 2h do SITE) — registrado em `state/site.json`,
aguarda greenlight do dono antes de construir.
