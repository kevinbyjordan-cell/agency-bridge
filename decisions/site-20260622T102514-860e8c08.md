# SITE · 2026-06-22T14:25:14Z · 860e8c08

**Run:** construir o heartbeat do SITE (ADS→SITE #6), pedido pelo dono ("faça o loop / pode seguir").

**Pré-commit (pedido do dono):** reconferi o bridge — única novidade do ADS = o **heartbeat automático dele**
(`aac7d64`, 10:07, só `state/gads.json`). **Nenhuma solicitação nova** dirigida ao SITE. (O #2 taboola tinha
um pedido leve já anotado pra próxima rodada do Clarity.)

**O que construí:** `pet-tooth-fairy/scripts/bridge-heartbeat.mjs` — espelha o `gads-cli/scripts/bridge-heartbeat.mjs`:
- **READ-ONLY.** Check 1: GET produção (`https://petoothfairy.com`) → site-up. Check 2: Firestore COUNT de
  `bookings` com `createdAt >= meia-noite (America/New_York)`. Escreve só o bloco `heartbeat` em `state/site.json`
  (preserva campos curados). Guard-band crítico **site_down** (exit-code 2). Flags `--no-git` / `--no-push`.
- Loader de `.env.local` próprio (o `process.loadEnvFile` do Node engole a `FIREBASE_PRIVATE_KEY` multilinha) +
  leitura de chave robusta (base64 ou `\n`-escapado), igual ao `firebase-admin.ts` do app.

**Verificação:** rodei `--no-git` 2×. **site-up = 200 OK ✅.** **bookings = BLOCKED** (fail-closed) porque o
`.env.local` local tem `FIREBASE_*` **vazios** (placeholders — o app roda em mock; creds reais só na Netlify).
Isso é o comportamento correto (nunca um 0 falso).

**Decisão do dono (AskUserQuestion):** **Opção A — pôr as creds Firebase reais no ambiente do heartbeat**
(em vez de criar um endpoint tokenizado). O dono coloca o segredo; eu não manuseio credencial.

**Pendências (do dono):** (1) `FIREBASE_*` reais no ambiente do heartbeat (ex.: `FIREBASE_PRIVATE_KEY_BASE64`);
(2) registrar a task 2h (`node scripts/bridge-heartbeat.mjs --no-push`). O classificador me trava de criar
persistência que auto-roda → agendamento é do dono.

**Boundary:** o script é read-only; nunca deploya/altera o site. **O arquivo do script vai por PR** no repo do
site (freio: SITE nunca dá push direto na main).
**Rollback handle:** reverter o commit do PR do script; o heartbeat não muda nada no site nem na conta.
