# SITE · 2026-06-22T14:46:07Z · b013a0fd

**Run:** verificação end-to-end do heartbeat #6 depois que o dono pôs as credenciais Firebase.

**Contexto:** dono rodou o script (que dei) pra puxar `FIREBASE_PROJECT_ID` / `FIREBASE_CLIENT_EMAIL` /
`FIREBASE_PRIVATE_KEY_BASE64` da Netlify e gravar no `.env.local` (gitignored). **Eu não toquei no segredo.**

**Bug encontrado e corrigido:** o teste do dono deu `MODULE_NOT_FOUND` — o `scripts/bridge-heartbeat.mjs`
estava só na branch do PR #2; quando voltei pra `main`, o arquivo saiu da working tree. Restaurei na working
tree (`git checkout site/bridge-heartbeat -- scripts/bridge-heartbeat.mjs`) pra poder testar.

**Verificação ao vivo (`--no-git`):** `site UP (200)` + **`bookings_today=0`** (`bookings_check=ok`, `ok=true`,
sem alertas). `bookings_since=2026-06-22T04:00:00Z` = meia-noite Eastern (EDT −4) → cálculo de fuso correto.
**Heartbeat #6 funciona end-to-end.**

**Segurança:** confirmei `.env.local` **gitignored** (`git check-ignore` → `.env.local`) → a chave nunca é
commitada. Nenhum arquivo versionado (site/bridge) contém o valor do segredo; só referências ao NOME da env var.
A saída do heartbeat mostra contagem/HTTP, **nunca** a chave.

**Pendências (do dono):** (1) **mergear PR petoothfairy-site#2** → torna o script permanente na `main` (hoje ele
só está na working tree porque restaurei; sem o merge ele some no próximo checkout). (2) registrar a task 2h.
(3) **rotacionar a chave de service-account** por precaução (apareceu em print/chat).

**Rollback handle:** o heartbeat é read-only; não há nada a reverter no site nem na conta.
