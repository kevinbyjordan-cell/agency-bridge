# SITE · 2026-06-22T08:45:56 · c227048c

**Run:** loop manual Fase 1 (acionado pelo dono: "faça o loop"). KILL=OFF verificado.

**O que fiz (nenhuma mudança aplicada no site — só análise + ponte):**
- Li o board + KILL. Os tickets `SITE → ADS #2/#3/#4` já estavam respondidos pelo agente de Ads.
- Agi nos tickets vivos `ADS → SITE #1` (fricção /book) e `#5` (0 conv / dead-click 35%).
- Investiguei o código ao vivo do funil: `app/page.tsx`, `Hero.tsx`, `StickyMobileCTA.tsx`,
  `QuizSection.tsx`, `PriceCalculator.tsx`, `app/book/page.tsx`, `JordanaChat.tsx`.

**Achados (as_of Clarity 3d 2026-06-22):**
1. O fluxo conversacional que o Ads pediu pro `/book` **já existe** = o quiz da home
   (`PriceCalculator`): cota em 3 toques, sem e-mail pra ver preço, booka inline.
2. **Furo do funil:** Hero (`Hero.tsx:90`) e barra mobile (`StickyMobileCTA.tsx:66`) mandam o
   clique primário pro `/book` (5 campos), pulando o quiz que converte melhor. Explica
   "/book = página #1 (95 visitas) e 0 booking".
3. **Dead-click 35% — candidatos:** imagem da capa (`Hero.tsx:149`), stat cards (`:181`) e badge
   flutuante (`:163`) têm afeto de clicável (`hover:scale`) mas não têm link/handler.

**Saída deste run:**
- Abri `proposals/site-20260622-cta-to-quiz.md` (status `proposed`) — Parte A: repontar CTAs pro
  `/#quote`; Parte B: tornar reais/remover o afeto falso dos elementos da capa.
- Atualizei o board: `ADS → SITE #1` e `#5` com a resposta do SITE; atualizei `state/site.json`.

**Não apliquei nada no site** (mudança de produção = gated). Implementação só após `approved` do
humano e via PR+preview (freio: SITE nunca dá push direto na main).

**Rollback handle deste registro de ponte:** SHA git do `agency-bridge` anterior a este commit.
**Idempotência:** se este run repetir, a proposta `site-20260622-cta-to-quiz.md` já existe → não recriar.
