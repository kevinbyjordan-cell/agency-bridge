# FB · 2026-06-22T00:00:00 · bootstrap

**Run:** criação do agente de Meta (Facebook/Instagram) Ads (acionado pelo dono: "criar super
agente de Facebook Meta Ads"). KILL=OFF verificado.

**O que fiz (sem gasto, sem campanha — só criação de infra do agente):**
- Absorvi 3 aulas (Ben Heath $200M + Pedro Sobral Meta MCP + Live 363) → `FACEBOOK ADS/_aulas/`.
- Escrevi a Mega Brain: `FACEBOOK ADS/docs/superpowers/reference/meta-ads-playbook.md`.
- Criei as skills `meta-ads-playbook` (+ Fase 2: setup, publico, criativos, campaign) no
  `.claude/skills/` da agência.
- Escrevi `FACEBOOK ADS/CLAUDE.md` (colaboração + freios), espelhando o do Google Ads.
- Registrei o agente no bridge: `state/fbads.json` saiu de `not-created-yet` → `bootstrapping`.

**Arquitetura (as_of 2026-06-22):** execução pelo **MCP oficial do Meta** (`mcp.facebook.com/ads`)
+ Ads Manager. Sem CLI própria nesta fase (a `fads-cli` é Fase 4 futura, só se os buracos do MCP
beta doerem). Disciplina de escrita: **dry-run lógico → criar PAUSED → humano ativa.**

**Saída deste run:**
- `state/fbads.json` ativado (schema espelhando `gads.json`).
- Nenhuma proposta aberta (Fase 1+2 é só infra; a 1ª campanha PAUSED do piloto pet-tooth-fairy vem
  depois, com aprovação humana).

**Não apliquei nada em conta de anúncio.** Conectar o MCP, montar e subir campanha = passos
seguintes, gated.

**Rollback handle deste registro:** SHA git do `agency-bridge` anterior a este commit; e remover as
skills `meta-ads-*` + reverter `state/fbads.json` para o placeholder `not-created-yet`.
**Idempotência:** se este run repetir, as skills e o `fbads.json` ativado já existem → não recriar.
