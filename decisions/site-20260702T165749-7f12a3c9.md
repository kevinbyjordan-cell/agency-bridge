# DECISION — SITE — 2026-07-02 — Ticket ADS→SITE #7, fixes 1+2 → PR #12

- **agent:** SITE
- **trigger:** ticket ADS→SITE #7 (06-30) com ⭐ prioridade explícita do dono:
  "atacar JÁ os fixes 1 e 2, via PR". Dono re-confirmou hoje na sessão ("sim faça isso").
- **status:** PR **petoothfairy-site#12** aberto — https://github.com/petoothfairy-png/petoothfairy-site/pull/12
  (aguarda merge do dono; nunca merge próprio em mudança de deploy).

## O que mudou (branch `site/ticket7-price-ladder-safety-proof`, commit c719382)

### Fix 1 — escada de preço honesta (mata o bait-bounce do "from $169")
- `Hero.tsx`: pill de preço → **"$169 cats & small dogs · up to $249 XL dogs"** +
  linha "Flat rate by size · no travel fee · $0 due today". Verificado mobile 375px sem overflow.
- `Comparison.tsx`: "From $169 flat" → **"$169–$249 flat, by size"**.
- `CityPageClient.tsx` (todas as city pages): pillar de preço com a mesma escada honesta.

### Fix 2 — bloco de prova segurança/eficácia perto do quiz
- Novo `SafetyProof.tsx` montado em `page.tsx` **logo abaixo do quiz** (`#safety`):
  1. Fully awake. Never forced. (sem sedativo/gás/IV; 10+ anos; lê a linguagem corporal)
  2. Does it actually work? (acima+abaixo da gengiva, polish, oral report; link → `#results`
     — âncora adicionada na seção BeforeAfter com fotos reais)
  3. Honest scope (não substitui o vet; extração/doença avançada → encaminha)
  4. If we can't clean, you don't pay (garantia de parada sem custo — cobre parcialmente o fix 3)
- **Regra seguida:** só claims que o site JÁ fazia (QuickAnswers/FAQ/Comparison/quiz).
  **Sem** "vet nomeado"/"vet-reviewed protocol" — não temos confirmação do dono (risco FTC);
  se ele confirmar, turbina depois.

## Verificação
`tsc --noEmit` limpo · render dev desktop+mobile ok · 0 erro de console ·
cadeia de âncoras `#quote → #safety → #results` confirmada no DOM.

## Próximos
- Dono mergeia PR#12 → Netlify deploya → **ADS** alinha copy do anúncio com a escada
  e mede CPA-por-booking pós-fixes (avisei no board pra NÃO alinhar antes do deploy).
- Fixes 3+4 (risk-reversal no CTA, prova/urgência acima da dobra) na próxima rodada; fix 5 (/book enxuto) depois.
